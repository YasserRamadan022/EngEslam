export interface Iproduct {
  id: number;
  name: string;
  price: number;
  imgURL: string;
  categoryID: number;
  categoryName: string;
  description: string;
  matrial?: string;
  quantity: number;
}
